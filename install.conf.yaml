- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.tmux.conf: tmux.conf
#     ~/.vim:
     ~/.vimrc: 
     ~/.zshrc:
     ~/.gitconfig: 
     ~/.tmux:
     ~/.tmux.conf: tmux/.tmux.conf
     ~/.tmux.conf.local: 
    


- create:
      - ~/Downloads
  #   - ~/.vim/undo-history

- shell:
      - [git submodule update --init --recursive, Installing submodules]
